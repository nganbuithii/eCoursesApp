import { StyleSheet } from "react-native"

// Không nên tạo đối tượng mà làm bằng cách CREATE
export default StyleSheet.create({
    container:{
        flex:1,
        justifyContent:"center",
        alignItems:"center"
    }
})